# 快速入门

放置计算机或采矿机器人并交互即可取得所有权。只有所有者或服务器管理员可以打开编辑器。选择语言、输入源码，然后点击“上传”。上传包包含唯一菜单会话令牌和单调递增序号；过期、重放、伪造、超限、距离过远或越权请求都会被拒绝。

潜行使用可写软盘可以复制设备的程序及 Shell 文件系统；普通使用软盘则会载入内容。预置介质只读。损坏或来自未来版本的软盘会整体拒绝载入，不会留下部分状态。

采矿机器人会消耗能量并保留历史动作延迟：前进与旋转需要五 tick，后退与采掘需要十 tick，扫描需要一 tick。机器人状态、待处理动作、姿态、采石场游标、库存和能量都能跨存档重启恢复。
