# 多方块结构

放置控制器并朝向目标方向，然后复现游戏内结构视图显示的每一层。结构部件只能归属一个控制器，重叠占用会被拒绝。

组装与解除均由服务端裁定。必要区块卸载时结构暂停，重新加载并验证后恢复；它不会强加载区块，也不会把卸载误判为解体。

控制器页面列出物品容量、用电与用热要求以及全部流体罐。请只使用声明的端口，避免输入或输出受阻。

压水堆和乏燃料池的尺寸、端口及安全操作另见[核电玩法](nuclear_power.md)。
