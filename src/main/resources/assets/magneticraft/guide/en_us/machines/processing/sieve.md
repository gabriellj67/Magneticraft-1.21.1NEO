# Sieve

The formed sieve processes advanced recipes with a server-authoritative inventory and output check. Inputs are consumed only when the full result can be committed.

Supply the energy and ports shown by the controller page. Stored progress survives saves and safely pauses during chunk unload.

JEI exposes the concrete sieve recipes when installed.
