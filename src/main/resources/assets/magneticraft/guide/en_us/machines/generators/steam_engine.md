# Steam Engine

Form the steam-engine multiblock exactly as shown in the structure view. Supply steam through its declared input tank; the controller converts accepted steam into native electrical output.

Generation pauses when the structure is incomplete, the input chunk is unavailable, steam is insufficient, or the electrical network applies backpressure.

Use the controller port summary for tank capacity and electricity access. The machine does not expose an implicit FE output.
