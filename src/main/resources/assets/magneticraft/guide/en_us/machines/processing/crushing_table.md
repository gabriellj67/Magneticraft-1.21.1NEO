# Crushing Table

Place one accepted item on the table, then strike it with the required hammer tier. Each server-validated hit advances the recipe and damages the hammer; the final hit replaces the stored item with its result.

Recipes belong to `magneticraft:crushing_table` and may define intermediate hit results. JEI shows the required tier and number of hits when installed.

Automation cannot bypass player hit validation or duplicate the stored stack.
